import React from "react";
import useURLParams from "../../hooks/useURLParams";

const DiscoSellers = () => {
  const { masterId } = useURLParams();
  return <div>DiscoSellers</div>;
};

export default DiscoSellers;
